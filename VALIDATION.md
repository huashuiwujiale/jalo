# 第一版验收记录（历史）

这是 0.1 版本的历史验收记录。当前 0.2 版本的文件引用、计划/审查模式、语法检查、轮次差异和安全回退验收见 [docs/reliability-validation.md](docs/reliability-validation.md)。

日期：2026-09-15，macOS，Node.js 22.19.0。

## 已完成

- `npm test`：**19 项通过，0 失败**。覆盖模型 SSE 分片、UTF-8、截断响应拒绝执行、LM Studio v1 管理、工具能力检测、文件编辑循环、异常参数、命令拒绝、等待确认取消、运行中取消与进程组退出、命令超时、路径越界、符号链接、硬链接、外部修改、原文备份、嵌套 AGENTS、上下文压缩及 SQLite 重启恢复。
- `npm run test:live -- --approve-fixture-command`：**通过**。使用本机 LM Studio 的 **Qwen3 8B**，读取临时样例，将 `sum(a,b)` 从减法修正为加法，生成差异，经测试脚本对固定只读命令的显式批准后执行 `node --version`，模型汇总完成。未修改业务仓库。
- Electron 开发版实际启动；检查三栏界面、模型设置、9 个语言模型的列表、默认模型选择、参数校验与保存。
- 桌面端实际任务运行已观察到 utilityProcess 完成工具调用并返回结果。
- 双击启动脚本通过 `zsh -n` 语法检查。

## 验收中修正

- 当前 LM Studio 接受字符串形式的 `tool_choice`，能力探测改用 `required`，加入对应回归测试。
- macOS `/var` 与 `/private/var` 路径别名规范化，避免只读样例命令被测试脚本误拒绝。
- 已修改文件再被外部更改时，阻止在原任务内合并差异，避免混入外部修改。
- 队列使用实际入队时间，继续旧任务不会插队。
- 设置校验将原始错误转换为中文提示。

## 尚未覆盖

- 未进行编译、打包、签名或安装包测试，遵守用户要求。
- 不承诺所有本地模型能稳定完成任意项目任务；仍需结合模型能力和上下文容量检查结果。
- 多任务 FIFO 的排序实现已检查，但未做大规模并发压力测试。
- 应用层文件检查不是对抗同用户恶意文件竞争的系统沙箱；终端命令明确按当前用户权限运行。
